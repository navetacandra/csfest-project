import { Router } from "express";

const router = Router();

/**
 * TODO:
 * Implement full mahasiswa administration route and connect to controller
 */

router.get("/");
router.post("/");
router.get("/:id");
router.put("/:id");
router.delete("/:id");

export default router;