import { Router } from "express";

const router: Router = Router();

/**
 * TODO:
 * Implement full schedule route and connect to controller
 */

router.get("/");
router.get("/current");

export default router;
